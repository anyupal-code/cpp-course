# VS Code
.vscode/

# Если файлы уже отслеживаются, очистить кэш
git rm -r --cached .

# Добавить всё заново (учитывая .gitignore)
git add .

# Закоммитить
git commit -m "Обновлён .gitignore, только .cpp и .h файлы"

# Отправить
git push origin main