// Lesson 02: Variables, branching and loops

/* 
1. Переменные (Variables)
Переменная — это именованная область памяти, хранящая значение. В C++ (в отличие от Python или JS) нужно обязательно указывать тип переменной.

Основные типы данных (примитивы):
- int — целые числа (от -2 млрд до +2 млрд обычно).
- double — числа с плавающей точкой (двойной точности).
- float — числа с плавающей точкой (одинарной точности, меньше памяти).
- char — один символ (в одинарных кавычках: 'A').
- bool — логический тип (true или false).
- std::string — строка текста (из библиотеки <string>).
*/

//Объявление, инициализация и присваивание:
#include <iostream>
#include <string>

int main() {
    // 1. Объявление (без значения - мусор в памяти!)
    int age;

    // 2. Инициализация (при создании)
    int count = 10;
    double pi = 3.14159;
    bool isReady = true;
    std::string name = "Alice";

    // 3. Присваивание (меняем значение позже)
    age = 25; 
    
    // auto (C++11) - компилятор сам выводит тип
    auto x = 5;      // int
    auto y = 3.14;   // double

    // const - значение НЕЛЬЗЯ изменить (константа)
    const int DAYS_IN_WEEK = 7;
    // DAYS_IN_WEEK = 8; // Ошибка компиляции!

    return 0;
}


// 2. Ветвления (Условные конструкции)
// Позволяют выполнять разный код в зависимости от условий.

if / else if / else // Важно: В C++ условие всегда должно быть выражением, возвращающим bool (или число, где 0 — это false, всё остальное — true).
int score = 85;
if (score >= 90) {
    std::cout << "Отлично!" << std::endl;
} else if (score >= 70) {
    std::cout << "Хорошо" << std::endl;
} else {
    std::cout << "Нужно подучить" << std::endl;
}


// Тернарный оператор (? :)
// Короткая запись для простого присваивания.

int a = 10, b = 20;
int max = (a > b) ? a : b; // max станет 20


// switch (множественный выбор)
// Работает только с целыми числами (int, char) и enum. Не работает со строками и float!

char grade = 'B';

switch (grade) {
    case 'A':
        std::cout << "Отлично";
        break; // Без break выполнение "провалится" в следующий case
    case 'B':
        std::cout << "Хорошо";
        break;
    case 'C':
        std::cout << "Удовлетворительно";
        break;
    default: // Если ни один case не подошел
        std::cout << "Неизвестная оценка";
}


// 3. Циклы (Loops)

/* Цикл for (классический)
Используется, когда точно знаешь количество итераций. */

// for (инициализация; условие; шаг)
for (int i = 0; i < 5; i++) {
    std::cout << i << " "; // Вывод: 0 1 2 3 4
}

// for на основе диапазона (Range-based for, C++11)
// Очень удобен для перебора массивов и коллекций.

int arr[] = {10, 20, 30, 40};

// Чтение (копирование элементов)
for (int x : arr) {
    std::cout << x << " ";
}

// Изменение (обязательно ссылка &)
for (int &x : arr) {
    x *= 2; // Теперь массив стал {20, 40, 60, 80}
}


// Цикл while
// Используется, когда условие зависит от внешних событий (например, ввод пользователя).

int number = 0;
while (number != 5) {
    std::cout << "Введите 5: ";
    std::cin >> number;
}

// Цикл do-while
// Отличается тем, что тело выполняется хотя бы один раз, даже если условие ложно (проверка идет в конце).

int secret;
do {
    std::cout << "Угадай число (1-10): ";
    std::cin >> secret;
} while (secret != 7);


/* Управление циклами (Важно!)
break — мгновенно выходит из цикла (прерывает его).

continue — пропускает оставшуюся часть текущей итерации и переходит к следующей итерации. */

for (int i = 0; i < 10; i++) {
    if (i == 3) continue; // Пропускаем 3 (не выводим)
    if (i == 7) break;    // Выходим на 7 (цикл заканчивается)
    std::cout << i << " "; // Вывод: 0 1 2 4 5 6
}


/* Сложный пример (Вложенные циклы + Ветвления)
Часто встречается на тестовых заданиях.*/

// Задача: Напечатать таблицу умножения от 1 до 3, но пропускать четные результаты.

#include <iostream>

int main() {
    for (int i = 1; i <= 3; i++) {
        for (int j = 1; j <= 3; j++) {
            int result = i * j;
            
            // Если результат четный - пропускаем вывод
            if (result % 2 == 0) {
                continue; 
            }
            
            std::cout << i << "x" << j << "=" << result << std::endl;
        }
        std::cout << "---" << std::endl; // Разделитель строк
    }
    return 0;
}


// Три главные ошибки новичков:

// Точка с запятой после if или for:
if (a > b); { ... } // Ошибка! Точка с запятой убивает условие. Тело выполнится всегда.

// Присваивание (=) вместо сравнения (==):
if (x = 5) { ... } // Ошибка! Мы присвоили x=5, а не проверили. Условие станет true (так как 5 != 0). 
// Лучше писать if (5 == x) - тогда компилятор поймает ошибку, если пропустишь '='.

//Забытый break в switch (смотри пример выше — выполнение "провалится" дальше).

// Современный C++ позволяет инициализировать переменные прямо внутри условия if или switch:

// Переменная init видна только внутри блока if
if (int value = getValue(); value > 10) {
    std::cout << "Value is " << value;
} else {
    std::cout << "Value is too small";
}
// Здесь value уже не существует
// Это улучшает читаемость и ограничивает область видимости.